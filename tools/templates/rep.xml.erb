<?xml version="1.0" encoding="UTF-8"?>
<mets xmlns="http://www.loc.gov/METS/" xmlns:xlink="http://www.w3.org/1999/xlink"
      OBJID="info:fcla/daitss/<%= @rep.ieid %>"
      LABEL="<%= @rep.package_id %>">
  
  <metsHdr CREATEDATE="<%= @rep.create_date.xmlschema %>">
    <agent ROLE="DISSEMINATOR" TYPE="ORGANIZATION">
      <name>FCLA</name>
      <note>tipr-1.0.0</note>
    </agent>
  </metsHdr>
  <% mynum = @rep.type=='ORIG'? 1 : 2 %>
  <amdSec>
    <digiprovMD ID="DPMD1">
      <mdRef LOCTYPE="URL" MDTYPE="PREMIS" xlink:href="tipr-rep-<%= mynum %>-digiprov.xml"/>
    </digiprovMD>
  </amdSec>

  <fileSec>
    <fileGrp>
<% @rep.files.each_with_index do |e,i| %>
      <file ID="file-<%= i %>" CHECKSUM="<%= e[:sha_1] %>" CHECKSUMTYPE="SHA-1">
        <FLocat LOCTYPE="URL" xlink:href="files/<%= e[:path] %>"/>
      </file>
<% end %>
    </fileGrp>
    <fileGrp USE='METADATA'>
      <file ID="REP-DPMD" CHECKSUM="<%= @rep.digiprov[:sha_1] %>" CHECKSUMTYPE="SHA-1">
        <FLocat LOCTYPE="URL" xlink:href="tipr-rep-<%= mynum %>-digiprov.xml"/>
      </file>
    </fileGrp>
  </fileSec>
  
  <structMap>
    <div>
<% @rep.files.each_index do |i| %>
      <fptr FILEID="file-<%= i %>"/>
<% end %>
    </div>
  </structMap>

</mets>
